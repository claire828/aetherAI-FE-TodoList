import { OrderbyNumPipe } from './orderby-num.pipe';

describe('OrderbyPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderbyNumPipe();
    expect(pipe).toBeTruthy();
  });
});
