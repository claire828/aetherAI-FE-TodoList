import { TimingFormatPipe } from './timing-format.pipe';

describe('TimingFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new TimingFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
